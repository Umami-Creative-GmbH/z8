"use server";

/**
 * Security Settings Server Actions
 *
 * Note: Most security features (2FA, Passkeys, Account Management) are now handled
 * client-side using Better Auth's built-in client plugins for better security and
 * proper authentication flow management.
 */

/**
 * Two-Factor Authentication Actions
 * Note: 2FA management is now handled client-side using Better Auth's twoFactor client plugin.
 * These server actions are no longer needed as the client methods provide secure, built-in functionality.
 */

/**
 * Passkey Actions
 * Note: Passkey management is now handled client-side using Better Auth's passkey client plugin.
 * These server actions are no longer needed as the client methods provide secure, built-in functionality.
 */

/**
 * Social Account Actions
 * Note: Account management is now handled client-side using Better Auth's built-in client methods.
 * These server actions are no longer needed as the client methods provide secure, built-in functionality.
 */
