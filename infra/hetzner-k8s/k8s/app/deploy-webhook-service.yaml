apiVersion: v1
kind: Service
metadata:
  name: deploy-webhook
  namespace: app-prod
spec:
  type: ClusterIP
  selector:
    app: deploy-webhook
  ports:
    - name: http
      port: 80
      targetPort: 8080
